query GetDashboard {
  dashboard {
    counts {
      total
      active
      planned
      paused
    }
    recentActivities {
      id
      projectId
      type
      message
      createdAt
    }
  }
}